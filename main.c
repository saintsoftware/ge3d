#include <iostream>
#include "image/image_lib.h"
// Under Develop at the moment...

/*
	Main
	empty loads nothing to printf
*/
	
char segment = "main_menu";

int main(){ 
	
	if(segment == "game") {
		// Load game
	}
	else if(segment == "main_menu") {
		// Load Main Menu
	}
	else if(segment == "loading_menu") {
		// Load Loading_menu
	}
}		
		
	
	
